#include <stdio.h>

int main()
{
    int i = 10; // Declare and initialize 'i' with 10
    int j = i;  // Declare 'j' and initialize with 'i'
    //%d is a format specifier 
    //%d is for int %f is floaat %c is for charcter 
    int a = 2, b = 3, c = 4, d = 5; // Declare and initialize multiple variables
    printf("the valus of i is :%d and the value of j is :%d\n ",i,j);
    printf("the valus of a is :%d and the value of b is :%d\n ",a,b);
    printf("the valus of c is :%d and the value of d is :%d\n ",c,d);
    return 0;
}